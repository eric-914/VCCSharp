<Window x:Class="DX8.Tester.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tester="clr-namespace:DX8.Tester"
        xmlns:converters="clr-namespace:DX8.Tester.Converters"
        mc:Ignorable="d"
        Title="DirectX 8 Tester" Height="250" Width="800">
    <Window.DataContext>
        <tester:TestWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <converters:ListToCountConverter x:Key="ListToCountConverter"/>
    </Window.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Joysticks:</Label>
            <Label Content="{Binding Joysticks, Converter={StaticResource ListToCountConverter}}"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding Joysticks}"/>
        <StackPanel Orientation="Horizontal">
            <Label>D-Pad:</Label>
            <CheckBox IsChecked="{Binding DPad.Left}">Left</CheckBox>
            <CheckBox IsChecked="{Binding DPad.Up}">Up</CheckBox>
            <CheckBox IsChecked="{Binding DPad.Right}">Right</CheckBox>
            <CheckBox IsChecked="{Binding DPad.Down}">Down</CheckBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Buttons:</Label>
            <CheckBox IsChecked="{Binding Button.X}">X</CheckBox>
            <CheckBox IsChecked="{Binding Button.Y}">Y</CheckBox>
            <CheckBox IsChecked="{Binding Button.A}">A</CheckBox>
            <CheckBox IsChecked="{Binding Button.B}">B</CheckBox>
        </StackPanel>
    </StackPanel>
</Window>
