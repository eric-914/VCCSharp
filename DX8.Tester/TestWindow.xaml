<Window x:Class="DX8.Tester.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tester="clr-namespace:DX8.Tester"
        xmlns:controls="clr-namespace:DX8.Tester.Controls"
        mc:Ignorable="d"
        Title="DirectX 8 Tester" Height="550" Width="420">
    <Window.DataContext>
        <tester:TestWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Margin" Value="0,6,20,0" />
        </Style>
        <Style TargetType="{x:Type controls:JoystickControl}">
            <Setter Property="Height" Value="200" />
        </Style>
    </Window.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Available Joysticks:</Label>
            <Label Content="{Binding Count}"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding AvailableJoysticks}"/>

        <Label>First Joystick:</Label>
        <controls:JoystickControl DataContext="{Binding LeftJoystick}"/>

        <Separator/>

        <Label>Second Joystick:</Label>
        <controls:JoystickControl DataContext="{Binding RightJoystick}"/>
    </StackPanel>
</Window>
