namespace VCCSharp.Models
{
    //--I'm guessing this is more a C/C++ interface to a struct than the struct itself
    // ReSharper disable once InconsistentNaming
    public struct IDirectDrawPalette
    {
    }
}
