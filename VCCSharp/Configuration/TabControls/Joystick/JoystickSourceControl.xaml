<UserControl x:Class="VCCSharp.Configuration.TabControls.Joystick.JoystickSourceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:joystick="clr-namespace:VCCSharp.Configuration.TabControls.Joystick"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="500">
  <UserControl.DataContext>
    <joystick:JoystickConfigurationViewModel/>
  </UserControl.DataContext>
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="JoystickSourceTabStyle.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <DockPanel>
    <TabControl SelectedIndex="{Binding InputSource}">
      <TabItem Header="Joystick">
        <joystick:JoystickAsJoystickControl DataContext="{Binding JoystickSource}"/>
      </TabItem>
      <TabItem Header="Mouse">
        <joystick:MouseAsJoystickControl DataContext="{Binding}"/>
      </TabItem>
      <TabItem Header="Keyboard">
        <joystick:KeyboardAsJoystickControl DataContext="{Binding}"/>
      </TabItem>
    </TabControl>
  </DockPanel>
</UserControl>
