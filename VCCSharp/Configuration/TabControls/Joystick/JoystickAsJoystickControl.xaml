<UserControl x:Class="VCCSharp.Configuration.TabControls.Joystick.JoystickAsJoystickControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:joystick="clr-namespace:VCCSharp.Configuration.TabControls.Joystick"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.DataContext>
    <joystick:JoystickViewModel/>
  </UserControl.DataContext>
  <UserControl.Resources>
  </UserControl.Resources>

  <StackPanel>
    <Image Stretch="None" Source="/Resources/joystick.ico" />
    <DockPanel>
      <Label DockPanel.Dock="Left">Windows Joystick:</Label>
      <Button DockPanel.Dock="Right" Click="RefreshList_OnClick" Content="Refresh List"/>
      <ComboBox Height="30"
                SelectedIndex="{Binding DeviceIndex}"
                ItemsSource="{Binding JoystickNames}"/>
    </DockPanel>

    <joystick:JoystickTestControl/>
  </StackPanel>
</UserControl>
